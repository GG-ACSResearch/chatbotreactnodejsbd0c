name: Trigger auto deployment for container-app-dd7pcpkar3uqa

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-dd7pcpkar3uqa-AutoDeployTrigger-7f6085e4-f97b-4504-a655-99c146226bbb.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPDD7PCPKAR3UQA_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPDD7PCPKAR3UQA_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPDD7PCPKAR3UQA_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPDD7PCPKAR3UQA_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPDD7PCPKAR3UQA_REGISTRY_PASSWORD }}
          containerAppName: container-app-dd7pcpkar3uqa
          resourceGroup: chatbotreactnodejsbd0c8cac
          imageToBuild: default/container-app-dd7pcpkar3uqa:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


